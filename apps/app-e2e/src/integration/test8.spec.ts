describe('test 8 template spec', () => {
    it('1. test passes', () => {
        expect(true).to.equal(true)
    })

    it('2. test passes', () => {
        expect(true).to.equal(true)
    })

    it('3. test fails', () => {
        expect(true).to.equal(false)
    })
})
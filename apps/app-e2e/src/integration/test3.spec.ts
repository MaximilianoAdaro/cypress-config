describe('test 3 template spec', () => {
    it('1. test fails', () => {
        expect(true).to.equal(false)
    })

    it('2. test passes', () => {
        expect(true).to.equal(true)
    })

    it('3. test passes', () => {
        expect(true).to.equal(true)
    })
})